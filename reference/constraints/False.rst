False
=====

Validates that a value is ``false``.

.. code-block:: yaml

    properties:
        deleted:
            - "False": ~

Options
-------

* ``message``: The error message if validation fails

See :doc:`True <True>`.
