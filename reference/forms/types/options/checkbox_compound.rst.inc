``compound``
~~~~~~~~~~~~

**type**: ``boolean`` **default**: ``false``

This option specifies if a form is compound. As it is not the case for checkbox,
by default, the value is overridden with the ``false`` value.
