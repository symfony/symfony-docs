block_name
~~~~~~~~~~~

**type**: ``string`` **default**: the form's name

Allows you to override the block name used to render display.
Useful by example if have multiple instances of the same form with differents names and that you need to personalize its render.
