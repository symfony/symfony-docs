{
    "minimum-stability": "dev",
    "prefer-stable": true,
    "config": {
        "platform": {
            "php": "8.4"
        },
        "preferred-install": {
            "*": "dist"
        },
        "sort-packages": true,
        "allow-plugins": {
            "symfony/flex": true
        }
    },
    "require": {
        "php": ">=8.4",
        "symfony/console": "^7.4",
        "symfony/process": "^7.4",
        "symfony-tools/docs-builder": "^1.0"
    }
}
